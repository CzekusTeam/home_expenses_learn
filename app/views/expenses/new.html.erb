<%= simple_form_for @form do |f| %>
<%= f.input :amount, as: :float %>
<%= f.input :period, collection: Period.all, include_blank: false %>
<%= f.input :category, collection: Category.all, prompt: 'Select Category' %>
<%= f.input :description, as: :text %>
<%= f.submit 'Add' %>

<% end %>